// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firestore: {
    apiKey: 'AIzaSyAGexxVdt9SMyWVf8B1tM9QH6_rm3cgB5c',
    authDomain: 'projectxen-alpha03.firebaseapp.com',
    databaseURL: 'https://projectxen-alpha03.firebaseio.com',
    projectId: 'projectxen-alpha03',
    storageBucket: 'projectxen-alpha03.appspot.com',
    messagingSenderId: '393951768006'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
